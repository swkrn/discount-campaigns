class Item {
    name: string
    price: number
    amount: number
    category: string

    constructor(name: string, price: number, amount: number, category: string) {
        this.name = name
        this.price = price
        this.amount = amount
        this.category = category
    }
}


export default Item